namespace CurlingSimulator.Core
{
    public enum CurlDirection
    {
        InTurn,   // stone rotates clockwise (curls left for Red throwing toward far house)
        OutTurn   // stone rotates counter-clockwise (curls right)
    }
}
